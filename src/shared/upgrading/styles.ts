import { makeStyles } from '@material-ui/core';

export const upgradingStyles = makeStyles({
  root: {
    height: '100%',
    width: '100%',
  },
  content: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
});
